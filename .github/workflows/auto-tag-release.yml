name: Auto Tag Release

on:
  push:
    branches:
      - main
      - master

permissions:
  contents: write

jobs:
  tag:
    uses: nikolareljin/ci-helpers/.github/workflows/auto-tag-release.yml@0.6.1
